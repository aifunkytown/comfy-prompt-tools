# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/
build/
dist/

# Editor/OS cruft
.vscode/
.idea/
.DS_Store
Thumbs.db

# Working files these scripts produce when pointed at real data - none of
# this belongs in the repo, only the tools that generate/consume it do.
*.csv
*.csv.tmp
rerun_log.csv

# Personal/local-only config overrides (same pattern as Claude Code's
# settings.local.json overriding settings.json) - never committed.
*.local.json
